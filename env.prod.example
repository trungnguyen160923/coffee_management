# ============================================
# PRODUCTION ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.prod and fill in real values
# DO NOT COMMIT .env.prod to Git!
#
# Usage on server:
#   cp env.prod.example .env.prod
#   nano .env.prod  # Fill in real values
#   docker compose -f docker-compose.prod.yml --env-file .env.prod up -d

# ============================================
# SPRING PROFILES
# ============================================
SPRING_PROFILES_ACTIVE=prod

# ============================================
# DATABASE (REQUIRED)
# ============================================
MYSQL_ROOT_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_USERNAME=root
MYSQL_DATABASE=coffee_db

# ============================================
# JWT SECRET KEY (REQUIRED)
# ============================================
# Generate a strong key: openssl rand -base64 64
JWT_SIGNER_KEY=CHANGE_ME_GENERATE_STRONG_JWT_KEY

# ============================================
# JVM MEMORY SETTINGS (Optional - defaults in docker-compose.prod.yml)
# ============================================
# JAVA_OPTS_GATEWAY=-Xms256m -Xmx512m
# JAVA_OPTS_AUTH=-Xms256m -Xmx512m
# JAVA_OPTS_CATALOG=-Xms256m -Xmx512m
# JAVA_OPTS_ORDER=-Xms256m -Xmx512m
# JAVA_OPTS_PROFILE=-Xms256m -Xmx512m
# JAVA_OPTS_NOTIFICATION=-Xms256m -Xmx512m

# ============================================
# FRONTEND BUILD-TIME VARIABLES (REQUIRED)
# ============================================
# These MUST be set when building images (in CI/CD or manually)
# They are "baked" into the frontend code at build time
VITE_API_BASE_URL=https://api.coffee-shop.com
VITE_AI_SERVICE_URL=https://api.coffee-shop.com/api/ai
REACT_APP_API_GATEWAY=https://api.coffee-shop.com/api

# ============================================
# EMAIL (SMTP) - Optional but recommended
# ============================================
# MAIL_HOST=smtp.gmail.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@gmail.com
# MAIL_PASSWORD=your-app-password

# ============================================
# OPENAI API KEY - Optional (for AI Service)
# ============================================
# OPENAI_API_KEY=sk-...

# ============================================
# GEOCODING API KEY - Optional (for Order Service)
# ============================================
# GEOCODING_API_KEY=your-geocoding-api-key

# ============================================
# NOTES:
# ============================================
# 1. All passwords and secrets should be strong and unique
# 2. Never commit .env.prod to version control
# 3. Frontend variables (VITE_*, REACT_APP_*) must be set at BUILD time
# 4. Backend variables can be set at runtime via environment variables
# 5. For production, use environment-specific values (not localhost)
# 6. Generate JWT key: openssl rand -base64 64

