package com.service.profile.dto.request;

import lombok.Data;

@Data
public class ShiftAssignmentUpdateRequest {
    // roleId removed - staff works with all their roles
    // This DTO is kept for backward compatibility but no fields are needed
}

